#!/usr/bin/env node
// T4 存根：ZCode 注册链已退役。原命令 discover-config 不再执行。
process.stderr.write(
  "✗ discover-config 已退役：dsh 版不支持 ZCode profile 注册链。\n" +
  "角色改由 subagent 实例的 persona / toolFilter / agentOptions 表达；派发走 workflow（provider/model 由 pick-profile 选型器输出）。\n" +
  "旧指令兜底：请改用派发层三件套（workflow / subagent / spawn_teammate），模型取样见 SKILL.md「模型阵营与取样规则」。\n"
);
process.exit(1);
