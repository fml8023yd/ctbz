# v1.7.0 统一裁决记录（6 路反审 → 修订定稿）

- 反审：6/6 回收（DS 渠道），阻塞 14 项、非阻塞 20+ 项，全部裁决完毕
- 用户预约：自动模式完成 v1.7.0 → Git + 阿里云；新增"自动模式推送条款"（直接加，未反审）

## 阻塞裁决（全部接受）

| 来源 | 阻塞 | 修订 |
| --- | --- | --- |
| 反审1 Q1 | 触发词三处复制无事实源 | 条目头部为唯一事实源；知识库.js 新增 index 重建目录/常用；常用从"闸"降为"排序器"（候选在目录全量上求并集） |
| 反审1 Q2 | 词形归一脆 | 脚本层确定性归一化（小写/去下划线/全半角）+ 每条目 2 形态别名槽 + 零命中同义扩展回填；向量化不进本版 |
| 反审1 Q5 | 零命中无出口 | 三分支：阻塞任务定义→问用户限一次；不阻塞→通用能力开工+缺口进待确认项；反审确认库缺→写自记库+缺口台账 |
| 反审2 阻塞1 | "识别空白"鸡生蛋+种子矛盾 | 表述改"词表交集"；种子恢复 5 层（PC/老客/FPD5_P1/样本口径/成熟门禁） |
| 反审2 阻塞5 | 版本检查错位（init create-only） | 独立 version-check 动作挂在知识库/意见箱入口前；storageRoot 解析不硬编码；**不删旧目录**（保留+标记已迁移） |
| 反审3 B0 | SKILL.md 无知识库接口=死库 | 补"知识库接口"章节（三读点+信任档+冲突闭环+写入门槛） |
| 反审3 B1 | 晋升路径缺失 | 待批→正式仅由用户显式命令 `草台班子 批准知识 <id>`；不做自动转正 |
| 反审3 B2 | 条目缺治理字段 | 补来源（日期+确认人/AI自记未确认）、风险等级（高中低）、内容类型（fact/experience/rule，自记禁 rule）；检索命中显式标注信任档 |
| 反审3 B3 | 夜间无人值守冲突分支 | 高风险冲突→该点记待确认跳过继续；低风险→现场证据优先+记录待确认继续 |
| 反审4 Q2 | 版本手传分叉（实测 1.6.0 vs 1.6.0-dev） | 脚本自解析 SKILL.md frontmatter；--version 降为 dev 覆盖 |
| 反审4 Q3 | init create-only 检查永不触发 | 同反审2 阻塞5：独立 version-check |
| 反审4 Q4 | 迁移语义模糊 | 按迁移时版本整月文件搬入 .ctbz/1.7.0/意见箱/；条目来源标签保留；先合并再删源 |
| 反审4 Q5 | 技术栈分裂（.py vs .js） | 意见箱改写 node scripts/意见箱.js，删 .py |
| 反审5 全部3项 | 种子/批入口/成本条款 | 与上述合并：种子 5 层、批准命令、**成本选型调用条款写入 SKILL.md**（×3 参数 --kind read --capacity 3 文档化） |

## 非阻塞裁决（全收）

- 子 Agent 分层授权：词典型预写契约；流程/事实型授读权限 + 缺口回传义务（NEEDS_CONTEXT 约定）
- "3 全文"改"同条重读≥2 次"才报警；库病诊断进自记库不进意见箱
- 快速模式保留读点①③、跳读点②
- 60 行限制只约束词典型整读候选；流程型强制锚点分段
- 自动晋升砍掉：常用.md 人工置顶；库指标.json 持久化（版本目录外）
- 冷归档机制（超 40 条/命中 3 次未引用→移归档/退出检索）；过期检测挂检索命中路径
- append-only 追加写；意见箱条目换行归一字面 \n
- 反馈模板：复述一次+行号指针，>160 字不进聊天
- 引用即核验、Betty 首跳界定、否决链附录留痕、显式"知识库不进 git"
- **用户新增（未反审）**：自动模式遇必须问用户的问题→ServerChan 推送；SendKey 初始化时检查+测试；未提供则跳过

## 版本与发布

- SKILL.md version 1.6.0-dev → 1.7.0；CHANGELOG 补 1.7.0；tag v1.7.0；Release；阿里云；ServerChan 通知
