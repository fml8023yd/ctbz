# ctbz 待办（滚动登记）

登记规则：每条 = `编号 | 来源 | 内容 | 影响 | 归属`。来源写版本号 + 回执路径或 commit；完成时把状态改为「已闭合」并附证据，不删除。

| 编号 | 来源 | 内容 | 影响 | 状态 |
| — | 2026-09-22 | **总路线图：`docs/修改计划.md`**（2.0.9 → 2.1.0 → 2.2.0 → 2.3.0 → 2.4.0；含 A 批 6 条 / B 批 17 条裁决清单） | — | 待裁决 |
|---|---|---|---|---|
| TD-01 | 2.0.8 L3 deepseek 卡点3；`SKILL.md` K4 自承 | 复命闸无强制触发点（`派发闸.mjs` / `反审.mjs` / 收尾自记均未挂链），「不过闸不得交付」是软约束 | 不跑就没人知道 → 复命闸形同可选 | 待办 |
| TD-02 | 2.0.8 实测（主会话复命被自己闸门拦下） | G8.2 黑名单按「包含」匹配：证伪实验里写「删掉 G8.2 推理词判定」被判红；描述该判据本身即误伤 | 合法自疑被误杀，用户会开始回避写具体实验 | 待办 |
| TD-03 | 2.0.8 L3 tencent B1 | G8.3 只认单一语序（`pass N` / `N fail`）；主文表格曾写 `<N> pass` 被 L3 逮到 | 文档已修，正则是否双向接受待定 | 待办 |
| TD-04 | 2.0.8 发布 | tag `v2.0.8` 指向 `2aee710`，其后 2 个纯文档 commit（`b86af57`/`1d20cac`）未进包 | 另一台电脑拉包得到的是修表前的版本 | 待办 |
| TD-05 | 2.0.6/2.0.7/2.0.8 三轮遗留 | 本地 `main` worktree 领先 `origin/main` 1 个 commit（`3361bb0`，09-17 既有，非本会话产物） | 远端 main 少一个既有 commit | 待办 |
| TD-06 | 2.0.7 L2 tencent | `发版.sh` 第 ④ 步硬编码 `git push origin main`，对 `ctbz-dsh` 线不适用；本轮及上轮均手工执行 tag/Release/阿里云 | 发版入口对 dsh 线不可直接用 | 待办 |
| TD-07 | 2.0.6 部署首跑实测 | `部署.js:65` 经 `/bin/sh -c node` 调 `发布检查.js`，PATH 无 `/usr/local/bin` 时必失败（`/bin/sh: node: command not found`） | 每轮部署须手工带 PATH | 待办 |
| TD-08 | 2.0.6/2.0.7/2.0.8 每轮均触发 | `发布检查.js` 只重算传入目录，`部署.js` 不回写仓库侧 `dependencies.lock.json` → 每轮「改文件→部署」都要手工补一次重算 | 漏一步则 `diff -rq` 永远非 0 | 待办 |
| TD-09 | 2.0.6 L3 | `deepseek-v4-pro` 未单独探活（`preflight.mjs` 只探 `deepseek-flash`） | 反审席位可用性只有间接证据 | 待办 |
| TD-10 | 2.0.8 L1 第四轮 | 回执 JSON 转义非法（`\s` 未双写）会卡整目录判不合格；已在提示词加约束 | 闸门是否在闸门侧容错待定 | 待办 |
| TD-11 | 2026-09-22 讨论 | **多方角色与拼图契合**：完工定义只覆盖「我交付了」，不覆盖「别人能不能接」；知识库 185 条中零条记录「角色/相关方」 → 提案 `docs/提案-多方角色与拼图契合.md` | 结构级缺口，待立项 | 待讨论 |
| TD-12 | 2026-09-22 讨论 | **记忆体系与路由**：自然语检索零命中（`模型上线` 0 条 / `特征开发 交付 上线 接口` 0 条）；触发词是 AI 自建索引；强制注入层只管输出格式 → 提案 `docs/提案-记忆与路由.md` | 找不到预期路线，同类问题反复重找 | 待讨论 |
| TD-13 | 2026-09-22 讨论 | **轨道与边界**：已知任务建轨道（节点+封闭分叉），未知任务走反审；前提=无轨道检测/有效期校验/写回 → 提案 `docs/提案-轨道与边界.md` | 无前提则变负收益（假装在轨道上） | 待讨论（前置 TD-12） |
| TD-14 | 2026-09-22 用户追问 | **内审闭环未闭**：①落盘默认 `<ws>/docs/内审`，而缺陷单 §5.2③ 原规格是「落 ctbz 仓库 `docs/内审/`」——记录散在各项目，负责 ctbz 开发的 Agent 看不到（实现偏离原规格，计划未标偏离）②上线至今**零条**（`docs/内审` 与 `.ctbz-record/内审` 均不存在）③`--level audit` 只拦「登记了没写完」，不拦「根本没登记」④无信箱/汇总入口，未与 `待办.md` 联动 ⑤触发全为自查项，闸门拒收/复命 exit 1/反审不接受等机械失败信号不自动生成内审 | 你要的「人工处理」入口不存在 | 待裁决 |
| TD-15 | 2026-09-22 用户追问 | **汇报协议（哑巴模式）无输出侧载体**：①三件套（极简输出 / 末段契约 / 哑巴模式）中只有「镜像两处逐字一致」有测试（`tests/主文锚点.test.mjs:125`），**我的实际输出零校验** ②「正文 ≤100 字」的豁免自授——例外只写「用户说展开」，我却自行判定「这次需要展开」，本会话多条回复远超 ③禁句族未列举，「已记录 / 先查现状 / 先取事实」等同族过渡与宣告句照写 ④三节各管一段（篇幅 / 末段 / 每行形态），无统一入口与优先级说明 | 输出协议的遵守度全靠自觉 | 待裁决 |
| TD-18 | 2026-09-23 用户感受 | **路径任意 / 浅尝辄止 / 交差不管**：①随便找条路（2.0.6–2.0.8 全程未调用 `知识库.js`，直到用户提示）②探索一下即收工（同步清单 R1 只列 6 处、实际 35 处）③交付即结束（自测出的缺陷只报不改，E2/E3）→ 提案 `docs/提案-路径与收口.md`；根因=我的「完成」判据是内部判据 | 随机路径 + 无穷尽判据 + 无续作线索；现有机制只管交付那一刻 | 待裁决 |
| TD-19 | 2026-09-23 用户质疑 | **版本号无取号规则**：2.0.6/2.0.7/2.0.8 三个 MINOR 级变更占了 PATCH 位；roadmap 又按「一项待办一个 minor」排号（2.1→2.5）＝号跟着任务数走。已在本会话定判据（MAJOR=删条款改语义／MINOR=对外变严／PATCH=不变严 + 自检两问）并落 `docs/修改计划.md` §〇；**待办项**：把该判据写进 `SKILL.md`「版本权威」条款 | 号不承载信息，读者无法预期 | **B2（v2.2.0）** |
| TD-20 | 2026-09-23 用户质疑牵出 | **三个版本有段无 tag**：`CHANGELOG.md` 有 `[2.0.4]`/`[2.0.5]`/`[2.0.6]` 段，但 `git tag` 无对应 tag（v2.0.7 起才补上）→ 与主文「版本权威：以 git tag 为准」自相矛盾。两条路：(a) 给这三段加 `[未发布]` 标注（推荐，历史已发布事实不可考）(b) 按当时 commit 补打 tag（会把未发布内容写进发布史） | 号与事实对不上，读者无法判断哪版真上线过 | **B2（v2.2.0）**，需裁决 (a)/(b) |
| TD-17 | 2026-09-22 用户纠正｜**已裁决（选项 a）** | **反审席位口径**：①`v4-pro` 底层被路由到 flash，**不再宣称与主进程互异** ②四路 = `hy4-preview-f` 或 `hy3`（腾讯）+ `dsv4 自身`（DeepSeek）+ `kimi-k2.8-preview`（月之暗面）+ `glm-5.3-flash`（智谱），**每家至少一个模型** ③裁决：**接受同源并明示**——声明改为「三路独立 + 一路同源」，DeepSeek 席仅提供独立上下文与采样，**不提供模型独立性** | 同模型自审未被真正拦住；独立性声明需改写 | **已闭合**（2.0.9 落地，commit `aac4ed8`，tag `v2.0.9`） |
| TD-16 | 2026-09-22 用户追问（**已按用户口径修订**） | **WB 渠道模型池**：网关 `/v1/models` 实测 **42 个**，ctbz 只注册 5 个。实测 11 个：免费档未用（`deepseek-v4-flash` 0、`hunyuan-2.0-instruct` 0、`hy3`/`hy4-preview-f` 0）；**列表里有死模型**（`minimax-m2.5` = `no_healthy_account`）→ 必须探活不能照抄；网关确实会改写 model 名（请求 `deepseek-v4-flash` → 返回 `deepseek-v4.1-flash`），响应头无上游线索 → 外部测不出路由。风险：WB 同 baseURL/同凭据/同限流池（本会话已实测席位返回 null、回执写盘截断） | 席位池扩容与探活 | 待裁决 |

## 已闭合（留档）

| 编号 | 内容 | 闭合证据 |
|---|---|---|
| TD-17 | 四路反审席位口径：改 `deepseek-flash`（明示同源）、声明「三路独立 + 一路同源」、六处口径 + 8 处测试同步 | 28/28 改动点落地；`tests/*.test.mjs` 100 pass / 0 fail；`--level l1` exit 0 且 review 块含 `"independence":"3 independent + 1 same-source"`；`--host-model glm-5.3-flash` 仍 exit 2；历史 2.0.6/2.0.7/2.0.8 计划仍 exit 0；L1 5 轮 + L2 双阵营 0 阻塞；tag `v2.0.9` + Release + 阿里云 |
|---|---|---|
| — | 暂无 | — |
