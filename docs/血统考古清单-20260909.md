# ctbz 血统考古清单（1.6.0 合并依据）

- 时间：2026-09-09
- 结论：**以引擎树（安装版）为唯一主文件基线**，成本选型层从发布/1.1.0 回迁，循环研究协议按引擎树原生"循环研究与开发"章节（round.start/finish 机制）为准，废弃其余拷贝。

## 血统清单

| 载体 | 行数 | 血统 | 核心差异 | 处置 |
| --- | --- | --- | --- | --- |
| **`.agents/skills/ctbz`（安装版=引擎树）** | 98 | 看板版 1.5.2 | dashboard 看板、round.start/finish 循环、讨论/执行两阶段、work 组 | **✅ 主文件基线**（ZCode 实际加载） |
| `/Users/maolong/ctbz`（git 仓库树） | 165 | 编号边界版 | #1-#18 边界、路由层、命令集【反审】【执行吧】、**cost-rules.json + pick-profile** | 精华回迁主文件后作为发布源 |
| `发布/草台班子-1.1.0` | 169 | 编号边界版+成本层 | **cost-rules.json v2 + pick-profile 的权威实现**在此 | 成本选型资产回迁后归档 |
| `发布/草台班子-1.2.0、1.2.1` | 44/50 | initialize 血统 | 角色清单+initialize 脚本，无成本层 | initialize 能力并入主文件，旧包归档 |
| `源码/`、`ctbz-pack`、`ctbz-pack-v2`、`.验证临时-*`×6、`旧发布-*`×2 | 各异 | 混合 | 历史打包产物与验证临时目录 | 保留不动（历史档案），标注废弃 |
| `发布/旧发布-b9401bdd`（176 行） | 176 | 编号边界版+ | 比 1.1.0 多 7 行 | 归档 |

## 关键发现

1. 引擎树**无** cost-rules.json 与 pick-profile（成本选型层权威实现在发布/1.1.0）；
2. 引擎树**有**原生的循环研究与开发协议（round.start/finish、项目看板、questions 决策）——与本会话早期设计的 research-loop.md 语义同源但载体不同，**以引擎树原生机制为准**；
3. 本会话早期写入的 research-loop.md 与"边界 #19"在全盘**均已不存在**（双树分叉+会话文件状态漂移所致），1.6.0 需按引擎树机制重新表述循环模式；
4. 编号边界版的 #16 反审规模、命令集【反审】【执行吧】、【草台班子竭诚为尊贵的您服务】起手式、主进程服务姿态条款需回迁主文件。

## 1.6.0 合并蓝图

- 主文件：`.agents/skills/ctbz/SKILL.md`（98 行为底），增补：版本权威（已入）+ 主进程服务姿态（已入）+ 命令集与反审规模（回迁）+ 三模式分流（快速/自动/循环映射到看板 mode normal/loop）+ 自动模式流水线与倒计时授权 + 意见箱入口；
- 资产回迁：cost-rules.json + pick-profile ← 发布/1.1.0（升级为 v3：5 规则+rescue）；doctor/team-state 等 scripts 以引擎树为底、pick-profile 为新增；
- 意见箱脚本：新建 `scripts/意见箱.py`；
- 发布源：合并后的引擎树整体入 git（`/Users/maolong/ctbz` 重建为引擎树镜像）。
