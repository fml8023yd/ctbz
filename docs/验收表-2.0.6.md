# ctbz 2.0.6 验收表（V1–V15）

计划: `docs/ctbz-2.0.6-派发闸与内审-计划.md`（sha256 `e2263d750b5dbfa442ca538ac2cc0de5abaf98dfbd1810ebcf1df80293db21bf`）
执行: T5 集成收口 Agent + F5 收口 Agent + 主会话收尾（D-3 仓库锁重算、E-1 复测、V8 判通过）
状态取值: 通过 / 未通过 / 待跑

## 全量测试与闸门

| 项 | 命令 | 结果 |
|---|---|---|
| 全量测试 | `PATH="/usr/local/bin:$PATH" /usr/local/bin/node --test 'tests/*.test.mjs'` | exit 0；A/B 前 `# tests 80 / # pass 80 / # fail 0 / duration_ms 2157.9`；A/B 后复跑 `# tests 81 / # pass 81 / # fail 0 / duration_ms 2005.9`（新增 F5 防回归 1 例） |
| 分文件测试 | 8 个 `tests/*.test.mjs` 逐个 `node --test` | A/B 前 17+5+3+3+6+8+20+18 = 80 pass / 0 fail；A/B 后 `主文锚点.test.mjs` 由 8 → 9 例，合计 81 pass / 0 fail |

## V1–V15

| 编号 | 验收项 | 状态 | 证据（命令 / 路径:行号） |
|---|---|---|---|
| V1 | 冷启动可发现 | 通过（人工核对） | 子串锚点实测：`grep -c -- 'scripts/派发闸.mjs' skills/ctbz/SKILL.md` → 1；`scripts/反审.mjs` → 1；`scripts/preflight.mjs` → 1；`.ctbz-record/反审/` → 4；`review_receipt` → 2；`spawn_teammate` → 4；`禁止` → 6。落盘路径：`grep -c '任务级/' skills/ctbz/SKILL.md` → 1（`SKILL.md:315`）、`grep -c '项目级/'` → 1（`SKILL.md:316`）。**人工核对**：L2 四路回执（`反审/任务级/T0..T4/*.json`）复述命令与路径，卡点见 V7 行；本表即冷读产物 |
| V2 | 闸门可拦 | 通过 | `node --test tests/派发闸.test.mjs` → 20 pass / 0 fail，含用例 `V2 无回执 → exit 1 且输出含可直接执行的补齐命令；补 4 份合格 → exit 0`（`tests/派发闸.test.mjs:151`） |
| V3 | 同模型自审被拦 | 通过 | 同上 20 pass；含 `V3 同模型自审被拦：model=deepseek-flash 拒；deepseek-v4-pro 过；--host-model 取席位模型 exit 2`（`tests/派发闸.test.mjs:200`） |
| V4 | 旧计划不可复用 | 通过 | 同上 20 pass；含 `V4 旧计划不可复用：改计划后仅最高轮回执被判 sha 不符`（`tests/派发闸.test.mjs:220`） |
| V5 | L2 隔离可判 | 通过 | 同上 20 pass；含 `V5 L2 隔离可判：implementer_camp == reviewer_camp 拒；不同阵营过`（`tests/派发闸.test.mjs:238`） |
| V6 | 启动自检 | 通过 | `grep -n 'preflight.mjs --json\|反审阻塞' skills/ctbz/SKILL.md` → `:12`（命令）、`:15`（「全部不可用 → 标『反审阻塞』并告知用户，不得静默开工」）；`node --test tests/主文锚点.test.mjs` → 8 pass / 0 fail（含 V6 用例；本轮新增 F5 防回归 1 例后为 9 pass / 0 fail）。实测留痕见「部署后校验」节 |
| V7 | 新鲜 Agent 测试 | 通过（人工核对） | 四路 L2 回执 `fresh_agent_test` 字段：`.ctbz-record/反审/任务级/T0/moonshot.json`（`blockers: []`，卡点 0）、`T1/zhipu.json`（4 条非阻塞，无阻塞级误导）、`T2/tencent.json`（7 条非阻塞 + 1 可接受）、`T3/moonshot.json`（`blockers: []`，卡点 0）、`T4/tencent.json`（5 条卡点，全部可指认于本计划文件与 §9 授权文本）。**人工核对**：卡点 0 或全部可指认于计划，无「无法开工」级卡点 |
| V8 | 不破坏既有 | 通过 | 测试面：`PATH="/usr/local/bin:$PATH" /usr/local/bin/node --test 'tests/*.test.mjs'` → `# tests 81 / # pass 81 / # fail 0`；既有命令 `【反审】` 仍在 `skills/ctbz/SKILL.md:187`、`【反审协议后开始工作】` 在 `:188`，措辞变更仅按 L2-1 回修统一席位数口径（L2 授权 F-2）。部署面：`发布检查.js "$PWD/skills/ctbz"` → 三步全过（重算仓库锁，与安装侧锁逐字相同）；`git commit aec23e0`；`部署.js` → **exit 0**（反向检测放行 → rsync → 发布检查 3/3 → `✓ 部署完成。`）；`diff -rq skills/ctbz ~/.agents/skills/ctbz` → **exit 0（无输出）**。见「部署收口」D-1/D-2/D-3/E-1 |
| V9 | 触发可自查 | 通过 | `node --test tests/内审.test.mjs` → 18 pass / 0 fail；触发节 `-` 列表项实测 `sed -n '/^## 触发/,/^## 三步/p' skills/ctbz/references/内审协议.md \| grep -c '^- '` → 8（≥5） |
| V10 | 归因层级可判 | 通过 | 同上 18 pass / 0 fail（含 L1 不合格 / L2 合格 / L2 不得判优秀 / L3 优秀 四态） |
| V11 | 禁止项被拦 | 通过 | 同上 18 pass / 0 fail（「加强意识」类修改项 → exit 1） |
| V12 | 同类第二次 | 通过 | 同上 18 pass / 0 fail（未带 `--prev` 拒；前条 `实现状态: 已实现` → 报「缺陷失效」升级 L3） |
| V13 | 硬门未被下调 | 通过 | `git show HEAD:skills/ctbz/SKILL.md \| sed -n '77p' \| shasum -a 256` → `d7bef505d4af949974b16948014d4fba8196c0c3156b823e7dcd173fd54d929e`，逐字等于计划 §9.11 快照 `d7bef505…`；`grep -c '所有计划实施前必须走反审协议' skills/ctbz/SKILL.md` → 1。工作树侧同一行现落 `SKILL.md:109`（T4 授权 +30 行 / F-4 +2 行位移），`sed -n '109p' skills/ctbz/SKILL.md \| shasum -a 256` → 同 sha；`node --test tests/主文锚点.test.mjs` → 8 pass / 0 fail（本轮 +1 例后 9 pass / 0 fail，硬门行断言逐字未动） |
| V14 | 状态目录不进仓库 | 通过 | `git check-ignore -v .ctbz-record/反审/x.json` → exit 0，输出 `.gitignore:2:.ctbz-record/`；`git status --porcelain \| grep -c '.ctbz-record'` → 0（grep exit 1，无命中） |
| V15 | L3 可判 | 通过 | `node --test tests/派发闸.test.mjs` → 20 pass / 0 fail，含 `V15 L3 三分支：缺阵营 → 1；checks 缺项 → 1；四阵营 + 四项齐全 → 0`（`tests/派发闸.test.mjs:263`）；真实 l3 联调见下节「L3 总项目反审」 |

## 闸门真实联调（本轮实跑）

| 层 | 命令 | 退出码 | 关键输出 |
|---|---:|---:|---|
| L1 | `/usr/local/bin/node skills/ctbz/scripts/派发闸.mjs --plan "$PWD/docs/ctbz-2.0.6-派发闸与内审-计划.md" --workspace "$PWD" --level l1` | 0 | `{"ok":true,"review_scale":"重","review_level":"l1","review_receipt":"/Users/maolong/Betty/草台班子/ctbz-dsh/.ctbz-record/反审/ctbz-2.0.6-派发闸与内审-计划/","review_camps":["deepseek","zhipu","tencent","moonshot"],"plan_sha256":"e2263d750b5dbfa442ca538ac2cc0de5abaf98dfbd1810ebcf1df80293db21bf","seats":8}` |
| L2-T0 | `… --level l2 --task T0` | 0 | `review_camps:["moonshot"]`，回执 `.ctbz-record/反审/任务级/T0/moonshot.json` |
| L2-T1 | `… --level l2 --task T1` | 0 | `review_camps:["zhipu"]`，回执 `.ctbz-record/反审/任务级/T1/zhipu.json` |
| L2-T2 | `… --level l2 --task T2` | 0 | `review_camps:["tencent"]`，回执 `.ctbz-record/反审/任务级/T2/tencent.json` |
| L2-T3 | `… --level l2 --task T3` | 0 | `review_camps:["moonshot"]`，回执 `.ctbz-record/反审/任务级/T3/moonshot.json` |
| L2-T4 | `… --level l2 --task T4` | 0 | `review_camps:["tencent"]`，回执 `.ctbz-record/反审/任务级/T4/tencent.json` |
| audit | `… --level audit` | 0 | `{"ok":true,"review_level":"audit","pending":0}`（`.ctbz-record/内审/` 不存在，无待办） |

## L3 总项目反审

| 项 | 状态 | 说明 |
|---|---|---|
| L3 四方整体复查（集成 / 调用名 / main 未污染 / 安装副本分支标识） | 通过 | 命令：`PATH="/usr/local/bin:$PATH" /usr/local/bin/node skills/ctbz/scripts/派发闸.mjs --plan "$PWD/docs/ctbz-2.0.6-派发闸与内审-计划.md" --workspace "$PWD" --level l3` → exit 0，输出 `{"ok":true,"review_scale":"重","review_level":"l3","review_receipt":"…/.ctbz-record/反审/项目级/ctbz-2.0.6-派发闸与内审-计划/","review_camps":["deepseek","zhipu","tencent","moonshot"],"plan_sha256":"e2263d75…db21bf","seats":8}`；四阵营回执 4 份齐全（`.ctbz-record/反审/项目级/<slug>/<camp>.json`），checks 四项齐全 |
| L3 条件项处置 | 通过 | 回执条件逐条落盘：B1「主文第 1 步缺 `--workspace` 照抄 exit 2」（tencent UX 视角「不接受」）→ 本轮 A-1 修正 + B 防回归断言；B2「安装侧 lock 与部署基线漂移、`diff -rq` exit 1」→ 本轮 D 重算 + 基线重建，处置见「部署收口」节；zhipu 条件「l2 缺 `--task <T号>` 形参未进主文与 `references/派发.md`」→ 本轮 A-2/A-4 落盘 |

## 部署后校验

安装副本: `/Users/maolong/.agents/skills/ctbz`

| # | 命令 | 退出码 | 真实输出 |
|---|---:|---:|---|
| ① | `diff -rq skills/ctbz ~/.agents/skills/ctbz` | 0 | **无输出**（两树 102 文件逐字全等）。过程：首测 exit 1 仅 `dependencies.lock.json` 一处，方向为**仓库侧**锁过时（A 改动后未重算）；主会话授权后重算仓库锁（`发布检查.js "$PWD/skills/ctbz"` → 三步全过）并 `commit aec23e0`，再跑 `部署.js` exit 0 → 本项转 0 |
| ② | `/usr/local/bin/node ~/.agents/skills/ctbz/scripts/派发闸.mjs --plan "$PWD/docs/ctbz-2.0.6-派发闸与内审-计划.md" --workspace "$PWD" --level l1` | 0 | `{"ok":true,"review_scale":"重","review_level":"l1","review_receipt":"/Users/maolong/Betty/草台班子/ctbz-dsh/.ctbz-record/反审/ctbz-2.0.6-派发闸与内审-计划/","review_camps":["deepseek","zhipu","tencent","moonshot"],"plan_sha256":"e2263d750b5dbfa442ca538ac2cc0de5abaf98dfbd1810ebcf1df80293db21bf","seats":8}` |
| ③ | `grep -n 'version:' ~/.agents/skills/ctbz/SKILL.md \| head -1` | 0 | `3:version: 2.0.6` |
| ④ | `/usr/local/bin/node ~/.agents/skills/ctbz/scripts/preflight.mjs --json` | 0 | `ok: 5 / calls: 5 / rows: M1=ok M2=ok M3=ok M4=ok M5=ok`（DeepSeek / 智谱 / 腾讯 ×2 / 月之暗面 四阵营全 ok） |

## 部署收口（本轮实跑）

| # | 环节 | 命令 | 退出码 | 关键输出 |
|---|---|---:|---:|---|
| D-0 | 首跑失败根因 | 首次 `部署.js`（调用方 PATH 无 `node`） | 1 | `发布检查` 步 `/bin/sh: node: command not found`——`部署.js:65` 经 `/bin/sh -c node` 调 `发布检查.js`，本机 `node` 仅存于 `/usr/local/bin`；该次 rsync 已落地、发布检查未跑、基线未更新，致二跑被守门拦下 |
| D-0b | 7 处漂移定性 | 7 文件逐一 `shasum -a 256` vs `git show HEAD:skills/ctbz/<f>` | — | 7/7 两侧同值 → **零手改**，漂移＝首跑半途失败（原证据见 B2 行） |
| D-1 | 半程修复锁与基线 | `PATH="/usr/local/bin:$PATH" /usr/local/bin/node skills/ctbz/scripts/部署.js --check-only` | 0 | `✓ ① lock 全量重算（按实际文件集合）` / `✓ ② verifyBundle 自测` / `✓ ③ dsh 版 status 冒烟` → `发布前置检查全部通过，可 commit/tag/Release。` / `✓ 部署完成。`；安装侧锁 98 → 101 项、0 不匹配；`~/Documents/.ctbz/部署基线.json` 重写为安装侧当前状态（102 项，mtime `Sep 22 13:28`） |
| D-2 | 完整部署 | `PATH="/usr/local/bin:$PATH" /usr/local/bin/node skills/ctbz/scripts/部署.js` | 0 | `== 反向检测（上次部署基线 vs 安装目录）==` → `（无基线或一致，放行）` → `== rsync 同步（源→安装目录，--delete 镜像）==` → `== 发布检查 ==` → 三步全 `✓` → `✓ 部署完成。` |
| D-3 | 仓库锁重算（主会话授权后） | `PATH="/usr/local/bin:$PATH" /usr/local/bin/node skills/ctbz/scripts/发布检查.js "$PWD/skills/ctbz"` | 0 | 三步全过 → `发布前置检查全部通过，可 commit/tag/Release。`；重算后仓库锁与安装侧锁 JSON 逐字相同（同一 `shasum`）；`git commit aec23e0` |
| E-1 | 部署后镜像比对 | `diff -rq skills/ctbz ~/.agents/skills/ctbz` | **0** | **无输出**——两树 102 文件逐字全等（首测 exit 1 的 `dependencies.lock.json` 差异已由 D-3 消除） |
| E-1b | 部署后镜像（除锁外） | `diff -rq --exclude=dependencies.lock.json skills/ctbz ~/.agents/skills/ctbz` | 0 | 无输出：除锁外逐字全等（锁消除后已无必要，保留作交叉证据） |
| E-2 | 安装副本真跑 | `/usr/local/bin/node ~/.agents/skills/ctbz/scripts/派发闸.mjs --plan … --level l1` | 0 | `"review_level":"l1"` / `seats:8` / sha `e2263d75…`；证明冷读者按 `SKILL.md` 的安装副本路径可跑通 |
| E-3 | 安装副本版本 | `grep -n 'version:' ~/.agents/skills/ctbz/SKILL.md \| head -1` | 0 | `3:version: 2.0.6` |

## 未通过 / 未跑项

| # | 项 | 状态 | 事实与证据 |
|---|---|---|---|
| B1 | `部署.js` 完整跑通 | 已闭合（本轮） | 原事实：两次实跑均 `exit 1`。首跑因调用方 PATH 无 `node`（`/bin/sh: node: command not found`，本机 `node` 仅存在于 `/usr/local/bin`）在「发布检查」步失败；补 PATH 后二跑被**安装侧漂移守门**拦下：`✗ 安装目录相对上次部署被手改（7 处，rsync --delete 会抹掉）`——按期停手，未强推、未手改安装目录、未改 `部署.js`。本轮：`部署.js --check-only` exit 0 → `部署.js` exit 0（走完全部四步），见「部署收口」D-1/D-2 |
| B2 | B1 漂移定性 | 通过（非手改） | 7 处逐一比对：`SKILL.md` / `references/内审协议.md` / `references/反审协议.md` / `references/派发.md` / `scripts/内审.mjs` / `scripts/反审.mjs` / `scripts/派发闸.mjs` 全部 `diff -q` 等于**仓库已 commit 内容**（`b95d217`），无任何一处偏离仓库。漂移来源＝首跑已执行 rsync 同步、仅后续发布检查步失败，基线（`~/Documents/.ctbz/部署基线.json`，99 项，mtime `Sep 21 17:25:58`）未随之更新 |
| B3 | 安装侧 `dependencies.lock.json` 未重算 | 已闭合（本轮） | 原事实：安装侧 lock 仍为 98 项（`Sep 21 17:25`），仓库侧已重算为 101 项（含新增 `scripts/派发闸.mjs`、`scripts/内审.mjs`、`references/内审协议.md`）；重算动作在 `发布检查.js` 步内，该步两次均未执行到。本轮 `部署.js --check-only` 执行到该步：`✓ ① lock 全量重算（按实际文件集合）` → 安装侧 101 项、逐条 sha 比对 0 不匹配。**反向残留**：仓库侧锁因本轮 A 改动而过时（2 条 sha），见「遗留项」L1 |
| B4 | 解除 B1 的唯一动作 | 已闭合（未走删基线路径） | 原提议「删除或重建 `~/Documents/.ctbz/部署基线.json`」为破坏性动作，按裁决未执行、未绕过守门。本轮改用非破坏路径：`部署.js --check-only` 在「发布检查」步后按安装侧实际文件**重写**基线（102 项，mtime `Sep 22 13:28`），守门基线与安装侧一致 → 完整部署反向检测输出「（无基线或一致，放行）」 |

## 遗留项

| # | 项 | 状态 | 事实与建议 |
|---|---|---|---|
| L1 | 仓库侧 `skills/ctbz/dependencies.lock.json` 过时 | **已闭合** | 事实：A 改动后仓库锁仍记旧 sha → `diff -rq` exit 1。处置：主会话授权扩 write-set 后重算（`发布检查.js "$PWD/skills/ctbz"`，三步全过）→ `commit aec23e0` → `部署.js` exit 0 → `diff -rq` **exit 0（无输出）**。结构性建议（未实现）：本环节每轮「改文件→部署」都要手工补一次仓库锁，建议后续把「重算仓库锁」并入 `部署.js` 收尾，否则同类残留会反复出现 |
| L2 | `部署.js` 经 `/bin/sh -c node` 调子进程 | 未修（未授权） | `部署.js:65` `execSync('node "…/发布检查.js" …')` 走 `/bin/sh`，PATH 无 `/usr/local/bin` 时必失败（首跑实证 `/bin/sh: node: command not found`）。建议改用 `process.execPath`（`发布检查.js:49/56` 已是该写法）。本轮只改文档与测试，未改脚本 |
| L3 | 反审席位/主进程模型的 `deepseek-v4-pro` 通道未单独探活 | 未验证 | `preflight.mjs --json` 只探 `deepseek-flash`（5 席位全 ok）。间接证据：项目级 deepseek 回执由 `deepseek-v4-pro` 产出（`.ctbz-record/反审/项目级/ctbz-2.0.6-派发闸与内审-计划/deepseek.json` 的 `provider`/`model` 字段），但该模型未单独探活 |

