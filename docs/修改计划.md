# ctbz 修改计划（滚动路线图）

制定：2026-09-22｜来源：待办 TD-01…TD-15 + 四份提案
状态：**待裁决**（未开工）｜维护方式：每新增一条 → 编 TD 号 → 归入某版本或「待归版」

## 一、总览

| 版本 | 主题 | 含待办 | 前置 | 定级 | 一句话 |
|---|---|---|---|---|---|
| 2.0.9 | 发布链与输出口径 | TD-04/05/06/07/08 + TD-15 | 无 | 重 | 把每轮手工补的发布动作固化，把汇报协议的豁免收死 |
| 2.1.0 | 反审口径与内审闭环 | TD-01/02/03/09/10 + TD-14 + **TD-17** | 无 | 重 | 改写反审独立性声明（三路独立+一路同源）；让「工作不及预期」有记录、有信箱、有机械触发源 |
| 2.2.0 | 记忆与路由 | TD-12 | 无 | 重 | 统一入口 + 路由表 + 路径记忆；修自然语零命中 |
| 2.3.0 | 轨道与边界 | TD-13 | **2.2.0** | 重 | 已知任务建轨道（节点＋封闭分叉），未知任务走反审 |
| 2.4.0 | 多方角色与拼图契合 | TD-11 | 无（建议排在 2.2.0 后） | 重（架构级） | 完工定义从「我交付了」改为「拼图放得下」 |

依赖图：`2.0.9 → 2.1.0 → 2.2.0 → 2.3.0`；`2.4.0` 可与 2.1.0 并行，但需 6 项裁决先落地。

## 二、分期任务契约

### 2.0.9 发布链与输出口径

| 项 | 内容 |
|---|---|
| 目标 | 发布链不再需手工补步；汇报协议豁免自授封死 |
| write-set | `skills/ctbz/scripts/部署.js`、`skills/ctbz/scripts/发版.sh`、`skills/ctbz/SKILL.md`、`~/.dsh/AGENTS.md`、`CHANGELOG.md`、`tests/主文锚点.test.mjs` |
| 改动 | ①`部署.js:65` 改 `process.execPath` 调子进程（TD-07）②`发版.sh` 第④步推**当前分支**而非硬编码 `main`（TD-06）③部署收尾自动重算**仓库侧**锁（TD-08）④汇报协议：豁免写死两种、篇幅可数、禁句族列举（TD-15）⑤tag 补包 v2.0.9（TD-04）⑥是否推 `main`（TD-05，需裁决） |
| 验收 | `部署.js` 裸环境（PATH 无 `/usr/local/bin`）exit 0；`发版.sh` 在 `ctbz-dsh` 线一次跑通；`diff -rq` exit 0 无需手工重算锁；全量测试全绿；两镜像块 diff 无差异 |
| 反审 | L1 四阵营 × 双轮（8 席）→ L2 → L3 |

### 2.1.0 反审口径与内审闭环

| 项 | 内容 |
|---|---|
| 目标 | 「工作不及预期」有记录、有信箱、有机械触发源；闸门误伤与容错修掉 |
| write-set | `skills/ctbz/scripts/内审.mjs`、`skills/ctbz/scripts/派发闸.mjs`、`skills/ctbz/scripts/反审.mjs`、`skills/ctbz/references/{内审协议,反审协议,派发}.md`、`skills/ctbz/SKILL.md`、`CHANGELOG.md`、`tests/{内审,派发闸,dsh-regression}.test.mjs` |
| 改动 | ①内审落盘回归原规格（ctbz 仓库 `docs/内审/`）②信箱索引 + 与 `待办.md` 联动（TD-14）③机械触发源：闸门 exit≠0 / 复命 exit 1 / 反审「不接受」→ 自动 `init` 内审待办（TD-14）④`--level audit` 增加「应登记未登记」检测（TD-14）⑤复命闸挂强制触发点（TD-01/K4）⑥G8.2 黑名单改「整句即推理词」（TD-02）⑦G8.3 双向语序（TD-03）⑧回执 JSON 容错或提示强化（TD-10）⑨`deepseek-v4-pro` 单独探活（TD-09）⑩**反审席位口径改写（TD-17，已裁决 a）**：四路 = `hy4-preview-f`/`hy3` + `dsv4 自身` + `kimi-k2.8-preview` + `glm-5.3-flash`；声明由「四路独立」改为「**三路独立 + 一路同源**」；`反审.mjs` 注释与 `派发闸.mjs` 同模型校验注释同步改口径（保留形式校验、去掉错误理由） |
| 验收 | 造一次闸门失败 → 自动出现内审待办；`docs/内审/` 有索引；`--level audit` 对未登记判不合格；G8.2 描述该判据本身不再被误伤；裸 `pass 99`／`99 pass` 均通过 |
| 反审 | 同上 |

### 2.2.0 记忆与路由

| 项 | 内容 |
|---|---|
| 目标 | 强制注入层管到「走哪条路」；同类问题不重复找 |
| write-set | `skills/ctbz/SKILL.md`、`skills/ctbz/references/路由表.md`、`skills/ctbz/scripts/知识库.js`、`CHANGELOG.md`、`tests/知识库.test.mjs` |
| 改动 | ①`SKILL.md` 顶部「开口三问」②全局路由表（活类型 → 先读哪个分区 → 再读什么）③零命中时把你的自然语回填条目别名槽（治 R1）④收尾把走通路径写进 `常用.md`⑤检索排序出「最该读的 3 条」 |
| 验收 | `知识库.js search "模型上线"` 命中 ≥1（现状 0）；`search "特征开发 交付 上线 接口"` 命中 ≥1（现状 0）；路由表在 3 个真实任务上跑通并留痕 |
| 反审 | 同上 |

### 2.3.0 轨道与边界

| 项 | 内容 |
|---|---|
| 目标 | 已知任务走轨道（节点＋封闭分叉）；未知任务明确走反审 |
| write-set | `skills/ctbz/references/轨道/<活类型>.md`（首批 3–5 条）、`SKILL.md`、`派发闸.mjs`、`tests/轨道.test.mjs` |
| 改动 | ①轨道文件格式（节点四要素：读什么/判什么/产物/失败怎么办）②无轨道检测（P1）③节点有效期校验（P2）④走完写回新分叉（P3）⑤首批轨道：特征衍生交付 / 模型上线 / 打分对账 |
| 验收 | 无轨道任务被标「新任务」且不降标准；轨道任务按节点跑通并有产物；有效期过期节点被判不新鲜 |
| 反审 | 同上 |

### 2.4.0 多方角色与拼图契合

| 项 | 内容 |
|---|---|
| 目标 | 完工定义从「我交付了」改为「拼图放得下」 |
| write-set | `skills/ctbz/SKILL.md`、`skills/ctbz/references/拼图.md`、`派发闸.mjs`（`--level fit`）、`tests/拼图.test.mjs` |
| 改动 | ①拼图表（角色/他要什么/我给他什么/他的约束/状态/证据）②完工定义三方（上游口径有据 ∧ 下游接口对齐 ∧ 横向无重复）③`--level fit`：有「待对齐」→ 不得标完工；「不适用」须附理由 ④假设行不得作为完工依据 |
| 验收 | 带「待对齐」的项目被闸门拦；「不适用（空话理由）」被拦；证据缺失的角色行只能填「待对齐（假设）」 |
| 反审 | L1 8 席 + 2 席 + 4 席（架构级，建议加第三轮） |

## 三、裁决清单

### A 批（阻塞 2.0.9 / 2.1.0，先答这 6 条）

| # | 决策 | 推荐 |
|---|---|---|
| D-01 | 内审落盘：(a) 默认 ctbz 仓库 `docs/内审/`　(b) 项目留档 + `push` 同步　(c) 两处都写 | **(a)** |
| D-02 | 闸门失败是否自动 `init` 内审（不靠 AI 自觉） | 是 |
| D-03 | 信箱目录：`docs/内审/` 还是 `docs/内审/inbox/` | `docs/内审/`（含索引） |
| D-04 | 汇报协议选 A/B/C | **B**（豁免写死 + 篇幅可数 + `播报:` 段） |
| D-05 | tag 补包 v2.0.9 时是否顺带补 v2.0.8 的两个文档 commit | 是（重打 v2.0.8 附件） |
| D-06 | 本地 `main` 领先 1 commit（`3361bb0`，非本会话产物）是否推 | 推（需要你说一声） |

### B 批（立项前再答，17 条）

| 版本 | 决策点 |
|---|---|
| 2.2.0 | 选 A/B/C；路由表位置；别名槽谁补；路径记忆落点；开口三问是否并入复命清单 |
| 2.3.0 | 是否立项及顺序；轨道粒度；封闭分叉谁定；无轨道降级方式；写回谁执行；轨道文件位置 |
| 2.4.0 | 拼图表粒度；证据门槛；角色清单谁给；「不适用」防滥用；角色口径卡；组织真值是否并入真值门 |

## 四、不做的

- 不动 `main` worktree 的既有内容
- 不放松四扇门与硬门行
- 不为「已声明已知限制」（K1–K5、P1–P3 前提）另开机械闸，除非 P1 落地
- 不改模型阵营表与 `pick-profile` 守恒顺序

## 五、成本提示（诚实项）

| 项 | 数字 |
|---|---|
| 5 个版本 × 反审 | L1 8 席 + L2 2 席 + L3 4 席 ≈ 14 次模型调用/版，合计 ≈ 70 次 |
| 本轮会话已消耗 | 16+ commit、反审回执 75 份、L1 共 12 轮 |
| 建议 | 纯文档版本可声明降级为「中」（4 席单轮），但须在计划里显式声明偏离，供 L1 复核 |

## 六、风险

| # | 风险 | 处置 |
|---|---|---|
| 1 | 版本铺太开，半年做不完 | 只承诺 2.0.9 + 2.1.0，其余按裁决逐版立项 |
| 2 | 2.2.0 之前建轨道（2.3.0）＝白建 | 依赖已写死，不跳版 |
| 3 | 2.4.0 若证据门槛不严 → 假账 | 证据门槛作为立项前置，不进实施 |
| 4 | 新提案持续涌入 | 每条先编 TD 号入 `待办.md`，不即时开工 |
