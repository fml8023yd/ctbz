# Changelog

## [2.1.0] - 2026-09-23

取证闸：把「凭印象编数」变成发不出去。

### 新增
- `scripts/派发闸.mjs --level l1` 增取证校验 E1–E6：缺 `## 现场核对` 小节、缺取证文件、`文件:行号` 不可达、数量断言未标 `↗` 锚点、锚点数字不在取证原文中 —— 任一命中即 L1 不合格。
- 取证原文落 `<ws>/.ctbz-record/取证/<计划名>.md`，由现场命令输出生成，**禁手写**。
- `SKILL.md`「派发前必跑」增第 0 步；「计划与验证标准」增「数字必须有出处」。
- 实现比计划严一档：`验收/不做/非目标` 节的跳过改为「遇同级或更高级标题即恢复扫描」（计划原文为整节跳过）—— 只影响其后小节的断言是否入闸面；由 `tests/派发闸.test.mjs` 的 C9 固化。

### 变更
- 版本 `2.0.9` → `2.1.0`（按取号判据：旧计划无取证小节即过不了闸 = 对外变严 → MINOR）。

## [2.0.9] - 2026-09-22

多渠道反审席位：对外口径由「四路独立」改「三路独立 + 一路同源」。

### 变更
- 反审席位 DeepSeek 取值 `deepseek-v4-pro` → `deepseek-flash`（v4-pro 底层路由至 flash，与主进程同源，已丧失独立性）；口径改「**三路独立 + 一路同源**」，独立性由腾讯/月之暗面/智谱三路保证。
- `scripts/派发闸.mjs`：`SEAT_TABLE.deepseek.models` 并列白名单 `["deepseek-flash","deepseek-v4-pro"]`（v4-pro 为 2.0.6/2.0.7/2.0.8 旧回执的历史兼容值）；新增 `SAME_SOURCE_MODELS`，`--host-model` 硬闸改「不得取非豁免席位模型」；同模型自审软校验按 camp 判定（deepseek 席豁免，其余三席保留）；`review` 块新增 `"independence":"3 independent + 1 same-source"`。
- 同步 20 处口径点：`scripts/{反审.mjs,pick-profile}`、`references/{反审协议,派发}.md`、`cost-rules.json`、`SKILL.md`；`cost-rules.json` history 保留 2026-09-21 史实并追加 `r2026-09-22` 更正注记。
- 同步 8 处测试点：`tests/{dsh-regression,派发闸,主文锚点}.test.mjs`、`tests/README.md`；V3 改四断言（deepseek 席同源放行、历史兼容值放行、其余三席同源仍拒、`--host-model` 取非豁免席位模型 exit 2）。
- 版本 `2.0.8` → `2.0.9`。

### 已知限制
- 反审结构实际由 4 路独立降为 3 路独立（RK1）；4 席中 3 席共用 `workbuddy` 网关（同 baseURL/凭据/限流池，网关抖动可同时打掉 3 席，RK2）。
- `deepseek-official` 单点同时影响主进程与该席（RK3）；`hy4-preview-f` 冷却时是否滑到 `hy3` 沿用 `pick-profile` 既有逻辑，本轮不新增兜底（RK4）。

## [2.0.8] - 2026-09-22

自证与自疑：把「请相信我」换成可证伪的证据。

### 新增
- `SKILL.md` 新增「## 自证与自疑（交付前必跑）」：自证优先于自述（结论必带命令或 `路径:行号`；复述既有事实不受限）、先证伪后确认、自疑必留痕（设计阶段落计划 `## 自疑（设计阶段）`，执行阶段落复命 `自疑:` 段）；并声明能力边界——自证管事实可信，不管需求正确。
- 复命清单新增第 4 段 `自疑:`（必填，≥3 条，不得写 `无`）。
- `scripts/内审.mjs 复命` 新增 G7/G8/G8b/G9：条数 <3 → 不合格；`证伪实验:` 为空或属推理词 → 不合格；`结果:` 不命中 `exit 码 / pass 数 / 路径:行号` 形态 → 不合格；三条重复 → 不合格；自疑条目沿用 G4/G5 扫描（指向本 skill 或只报不改 → 不合格）。

### 变更
- 复命清单由三段变四段（`自主延伸` / `自主修复` / `待裁决` / `自疑`）；`SKILL.md` 模板句「三段均为必填」→「四段均为必填」。
- `tests/主文锚点.test.mjs` 版本断言改为与 `CHANGELOG.md` 首个版本段头交叉校验（一次修掉「升版本必红」这一类）。
- 版本 `2.0.7` → `2.0.8`。

### 已知限制
- G8 只验**形状**（箭头 / 非推理词 / 结果形态），不验证伪实验是否真跑过；真伪由 L1/L2 反审抽查兜底（K5）。

## [2.0.7] - 2026-09-22

边界延伸与复命闸：把「不确定就问」这个省力默认路径封掉。

### 新增
- `SKILL.md` 新增「## 边界延伸（做得多、说得少）」：默认做三判据（有条款出处 / 可回滚或可验证 / 不触四扇门）、复命门槛、问的唯一准入、发现缺陷默认修、拿不准取可逆一半、汇报双轨与探索树、复命清单模板。
- `scripts/内审.mjs` 新增 `复命` 子命令：机械校验复命清单——每个「待裁决」须带 `准入:`（四扇门/不可逆/用户要求二选一），出现指向本 skill 的 `出处:` 即判不合格。
- `references/内审协议.md` 触发节 +2 条：有明确下一步却停下复命；把自己发现的可修缺陷转成待裁决。

### 变更
- `tests/主文锚点.test.mjs` 的硬门行校验改为按内容定位，不再依赖行号算术（原算术在插入新节后必然失效）。
- 版本 `2.0.6` → `2.0.7`。
- 已声明已知限制 K1–K4（K1–K3：复命闸为启发式文字闸，不穷举绕过路径，绕过即转内审触发项；K4：复命闸暂无强制触发点，「不过闸不得交付」靠主文约束，挂链属下版），见 `SKILL.md` 「边界延伸」节。

## [2.0.6] - 2026-09-22

把「忘记审」变成「发不出去」：新增派发闸与内审协议。

### 新增
- `scripts/派发闸.mjs`：派发前硬闸（L1 四路回执 / L2 任务隔离 / L3 总项目复查 / audit 内审待办），逐项校验回执 schema、席位表、`plan_sha256`，通过时输出可粘贴的 `review` 块。
- `scripts/内审.mjs`：`init` / `check` / `link` 三命令，六字段校验 + 归因层级（L1 不合格 / L2 合格 / L3 优秀）+ 禁止项拦截 + 同类第二次强制挂链。
- `references/内审协议.md`：自省与缺陷回流协议全文。
- `SKILL.md` 新增「派发前必跑（硬闸）」「会话启动自检」「内审协议」三节，并在三层反审协议处补定级表（`review_scale` 为计划必填字段）。

### 变更
- `scripts/反审.mjs`：骨架生成时按 `--workspace` 落 4 份空回执模板（`verdict: null`，区分「没写」与「没审」）；骨架头注释固定写出回执落盘路径与闸门校验命令；新增 `--gate` 直连闸门。既有 stdout/stderr 格式不变。
- `~/.dsh/AGENTS.md`：澄清「ZCode 不适用」仅限 profile 注册链，反审席位链照常使用；补会话启动自检。
- 定级表统一：单文件小改 ≥3 / 模块级 4 / 架构级 8（4 阵营 × 双轮）。

## [2.0.5] - 2026-09-21

输出收紧：新增「哑巴模式」——默认零散文，只许结构行；语气从硬。

### 新增
- `## 哑巴模式`：每行必须是 表格行 / 编号项 / `键:值` / 代码 / `路径:行号` / 命令；散文仅在结构表达不了时用。
- 语气从硬：禁「可能/也许/似乎/大概/或许/建议考虑」；不确定标「未验证」。
- 四类产物分规：写 .md / 写代码（指向 ponytail）/ 写正式报告 / 汇报（聊天回复）。
- 唯一例外：ctbz 相关回复保留起手式。
- 同一文本以 `<!-- 哑巴模式:BEGIN/END -->` 标记逐字写入 `skills/ctbz/SKILL.md` 与 `~/.dsh/AGENTS.md`。

## [2.0.4] - 2026-09-21

回复末段契约 + dsh 线首次落盘。版本号由 `2.1.0-dsh` 改为 `2.0.4`，宿主标识行保留。

### 新增
- `## 末段契约`：每条含文字的终结回复，末段以 `状态：` 起头，只写现状 / 风险 / 待裁决（缺项写「无」）。
- 形状判据：一行装得下就一行；并列子项用 1/2/3；≥3 组对照才用表；不为凑形状造内容。
- 例外清单：起手式单句、事故三行、用户「展开/看细节」、逐条审计清单、纯闲聊、起手式即整条回复。
- 同一文本以 `<!-- 末段契约:BEGIN/END -->` 标记逐字写入 `skills/ctbz/SKILL.md` 与 `~/.dsh/AGENTS.md`，可用 sed 区间 diff 检出漂移。

### 变更
- `version: 2.1.0-dsh` → `2.0.4`；同步 `docs/迁移说明.md`、`docs/与main差异清单.md` 的版本标识。
- 一并提交 T1–T9 dsh 迁移未提交改动（ZCode 注册链退役、派发层、三层反审协议、健康账本）。

## [2.0.3] - 2026-09-17

需求理解与持续改进闭环补强。

### 新增
- 需求诊断增加意图置信度、替代解释、反事实检查和真实问题回指。
- 任务增加停止条件、预算、最大回修轮数和阶段可见性。
- 最终验收明确功能完成度、问题解决度、实际可用度。
- 用户实际反馈回流知识库、路由和后续计划。
- 增加“版本升级=能力盘点”规则，部署后核对 CHANGELOG/git diff，并报告本机挂载项。

### 本机挂载项
- Codex 每日版本检查任务需由宿主自动化注册；ZCode 可调用 `skills/ctbz/scripts/自动更新检查.mjs`。

## [2.0.1] - 2026-09-16

需求理解、激进并发与质量闭环协议补强。

### 新增
- 增加需求诊断门：区分字面需求、真实问题、目标结果、非目标和成功证据。
- 增加需求覆盖与长期短板两项反审必答题。
- 规划激进并发调度：资源池、依赖就绪事件、write-set 冲突阻断和独立 worktree。
- 规划子进程优先执行与独立质量复核：输出《我看到了什么内容》，交叉验证、打回、回修、复检直到通过。
- 增加普通/自动模式开工授权门；快速模式保留直接执行。
- 规划知识全量捕获，先进入【未分类】再分层治理。

### 边界
- 本版本记录协议和实施方案，不宣称并发调度内核、CLI 门禁或知识捕获入口已经实现。

## [未发布] - 2026-09-10

### 变更
- **团队派发硬约束（所有权）**：快捷初始化并激活后，执行类工作（写文件、改代码、跑验证命令，含小修复）默认派发 ctbz 团队 profile；general-purpose、Explore 等宿主通用 agent 不得替代或混用。主会话直做仅限纯讨论问答、用户明确免派、以及未激活/select 拒绝时的阻塞报告（不得降级用通用 agent）。
- **收窄主会话直做边界**：「普通已授权小修复直接执行」改为「已激活会话中的执行类工作（含小修复）按所有权约束派发团队」。
- **初始化快路径**：已 prepare 的机器新会话只需写 loaded JSON + activate 一步激活，不得重复 prepare；status 报 drift 时先核对 installRoot 区分副本劫持与宿主刚重写配置的瞬时漂移。
- **prepare 携带已激活会话**：新旧 bundle 的 profile 名称集合一致时，自动把 activeSessions 携带进新 generation（状态文件与 team-state 同步标记 `carried-from:<旧代state>`），维护批次不再踢出在用会话；名称集合变化时不携带，按原流程要求新会话激活。
- **adopt-run 跨代接管**：新增 `--adopter-state-dir`。权限按当前代会话校验，`--state-dir` 指向旧代并核验 manifest 溯源；接管后旧 manifest 冻结为 cancelled 并注明去向，续跑 manifest 重绑当前代（指纹/团队配置/路由）并记录 adoptions 溯源；终态 run 不可再接管。
- **prepare 全候选保护**：未指定 `--selection` 且候选超过 12 个时直接拒绝（提示提取现行 modelRef 或显式 `--allow-all-candidates`），防止误生成全量 profile 并清空已激活会话（2026-09-11 17:28 320-profile 事故根治）。
- **初始化判定阶梯**：「草台班子初始化」按序判定——initialized/awaiting 均只写名单+activate，禁止重复 prepare；仅 drift（核对 installRoot）或首次才 prepare 且必须带 selection。select 的未激活报错改为自愈指引（写名单+activate，无需重新 prepare）。
- **dashboard work.create**：独立 work 组登记新增 `work.create`（已存在则拒绝 WORK_EXISTS，更新用 work.upsert）；未知 action 的报错列出全部合法 action 清单。项目看板.md 补登记顺序：先 work.create 后 scope.claim。
- 新增 tests/governance.test.mjs（携带激活、跨代接管、work.create 三组回归，隔离夹具真实 CLI）。

## [1.6.0] - 2026-09-09

> ⚠ 已知缺陷补记（2026-09-10）：本版本随附的 dependencies.lock.json 含幽灵哈希（SKILL.md 条目四代未重算、虚挂 dashboard-example），全新安装会在 verifyBundle 拦截——已在 v1.8.1 根治（发布前置钩子 + lock 全量重算）。

首个以 git 为版本权威的正式发布（tag v1.6.0）。

### 新增功能
- **循环研究模式**：时间盒内自主迭代研究（轮次讨论→成本选型→strict 派发→回收→轮次日志），支持回滚、方向黑名单、干预接口，到点收官出报告。
- **成本选型 5 规则 + 跨渠道救生**：R1 免费时段（降档即冗余）→ R2 限免模型 → R3 主进程同源（尽力尝试级）→ R4 同厂商默认档 → R5 同厂商次级档 → rescue 跨渠道兜底（实测 DS 渠道配额独立可用）。
- **快速模式**：可行性验证优先，跳过反审，产物可清理，不缩减反审规模。
- **自动模式**：设计→反审→测试→开发→本地集成验证→推送前准备全链自主；开启命令即一次性反审授权（记 manifest）；推送依赖用户在场设备码授权。
- **意见箱**：`草台班子 意见箱 <内容>` 原封记录至 `~/Documents/意见箱/`，按月分文件，仅记录未采纳。
- **主进程服务姿态**：多任务并行设计（约 50% 并行度）、快分发后台跑、主进程时刻待命。
- **汇报起手式**：固定"草台班子竭诚为尊贵的您服务～"。

### 优化改进
- SKILL.md 三版归一：dashboard 看板版 + 编号边界版 + 循环研究合并为单一主文件（以安装版为基线）。
- 成本选型规则集 v2→v3：新增 cost_class 分级（free/paid-light/paid-heavy）与 --main-profile 参数。
- 版本权威确立：以 git tag 为唯一版本来源，本机目录为部署副本。

### Bug 修复
- 修复双树/四谱系分叉导致的"文档升级行为未变"漂移（血统考古清单见项目归档）。
- 修复免费档配额耗尽后无兜底通道的问题（跨渠道 rescue 实测可用）。

## [1.0.0 - 1.5.2] - 历史分代补记（未独立发布）

- ca29ff9（2026-09-05）：多 Agent 调度 + 成本选型层初版（v2 三规则），GitHub 首发。
- 1.1.0（2026-09-05）：修复状态契约与便携成本选型（发布包血统）。
- 1.2.0-1.2.1（2026-09-07）：initialize 血统发布包。
- 1.5.0-1.5.2（2026-09-07/09）：引擎树看板版（dashboard、round.start/finish 循环协议、work 组）。
- 以上均为本机分代演进，未打 tag；1.6.0 起合并为单一血统。

## [1.7.0] - 2026-09-09

> ⚠ 已知缺陷补记（2026-09-10）：本版本随附的 dependencies.lock.json 含幽灵哈希（SKILL.md 条目四代未重算、虚挂 dashboard-example），全新安装会在 verifyBundle 拦截——已在 v1.8.1 根治（发布前置钩子 + lock 全量重算）。

知识库体系正式版 + 治理闭环（6 路反审 14 阻塞全裁）。

### 新增功能
• 知识库三库分权：正式库（用户批准）> 待批区（标"待批"）> 自记库（标"未经验证"）；检索命中显式标注信任档。
• 检索协议：先分析后带缺口查；触发词交集（确定性归一化+别名槽+同义扩展回填）；常用.md 为排序器非闸门；零命中三分支。
• 构建知识库入口（幂等冷启动，种子首批 5 层：PC/老客/FPD5_P1/样本口径/成熟门禁）。
• 意见箱迁移：~/Documents/.ctbz/{版本}/意见箱/，node 版脚本（版本自解析 frontmatter），反馈复述模板。
• 初始化版本检查：独立 version-check 挂知识库/意见箱入口，storageRoot 解析。
• 冲突闭环：自查→限问一次→裁定回写；无人值守分支（高风险跳点继续、低风险现场优先）。
• 自动模式推送条款：必须问用户时 ServerChan 推送，SendKey 初始化检查，未配置跳过。
• 主进程服务姿态第 4 条：文字经济学。

### 优化改进
• 条目治理字段：来源/风险等级/内容类型（自记禁 rule）/scope/有效期/需现场核验/易错点。
• 晋升协议：待批→正式仅由"草台班子 批准知识 <id>"；自动晋升砍掉，常用人工置顶。
• 冷归档与过期检测挂检索命中路径，无扫描器。

### Bug 修复
• 修复版本手传分叉（1.6.0 vs 1.6.0-dev 实测脏数据）。
• 修复意见箱 js/py 引用漂移与技术栈分裂。

## [1.7.1] - 2026-09-09

> ⚠ 已知缺陷补记（2026-09-10）：本版本随附的 dependencies.lock.json 含幽灵哈希（SKILL.md 条目四代未重算、虚挂 dashboard-example），全新安装会在 verifyBundle 拦截——已在 v1.8.1 根治（发布前置钩子 + lock 全量重算）。

人设 description + 调度健壮性（采纳另一个 Agent 的实战复盘）。

### 新增功能
• 人物设定 description：懂领导所想的骨干——先看蓝图再动手、交付前自问"字面完成≠真解决"、上下游接口意识、时刻待命。
• 模型健康账本：~/Documents/.ctbz/模型健康.json，从限额报错解析冷却截止，派发前过滤冷却中模型，过期自愈。
• 反审规模分级：按改动面缩放（小改 2-3、模块级 4-6、架构级 6+），不再一律 6 个。
• 文字经济学执行自检："删掉一半还成立吗？成立就删"。

### Bug 修复
• 修复 write-set 按关注点切分导致的编译死锁（接口与实现必须同一 write-set）。

## [1.8.0] - 2026-09-09

重构模式 + 用户偏好体系（settings.yaml）+ 模型健康账本落地。

### 新增功能
• 重构模式（十步协议）：边界→理清(行为清单+意图动因)→基线固化(golden)→新方案→反审(主审方案质量)→表达+叫停窗口→可编译单元派发→备份+原子替换集成→行为对照验证→停机条款→汇报。【自动重构】进命令集。
• settings.yaml 用户偏好体系：权限边界（执行档位/计划超时自动执行档位/可写目录）、触点（ServerChan 环境变量名，key 不落盘）、知识库偏好；初始化访谈+旧版继承（只问变化项）。
• 模型健康账本脚本：从限额报错解析冷却截止、select 前过滤、过期自愈。

### 优化改进
• 1.7.0 推送条款改读 settings；计划超时档位联动推送提醒。
• 知识库/意见箱/偏好三体系路径归一：数据按版本目录、偏好跨版本。

### Bug 修复
• 修复 settings 嵌套 YAML 解析与 env 继承结构（实测 serverchan_key_env 双保险生效）。

## [1.8.1] - 2026-09-10

发布流程根治 + 倒计时接线（问题报告：另一个 Agent 排查 ctbz1.8.0 初始化依赖检查失败）。

### 新增功能
• 发布前置钩子 scripts/发布检查.js：打 tag 前强制 lock 全量重算 + verifyBundle 自测 + initialize 冒烟，杜绝幽灵哈希再发布。
• 倒计时接线：settings"计划超时自动执行">0 即倒计时默认档——计划给出后自动启动限时授权（到点 ServerChan 提醒、回滚锚点先行），显式口令可覆盖，档位 0 恢复显式触发。

### Bug 修复
• 修复 dependencies.lock.json 幽灵哈希（fa73836…四代未重算）+ 虚挂 dashboard-example 条目——该缺陷导致 v1.6.0~v1.8.0 全新安装必在 verifyBundle 拦截。lock 改为按实际文件集合全量重算（84 文件）。

## [1.8.2] - 2026-09-10

草台班子安家：开发工作区正式迁入 Betty 树，开发/运行分离。

### 新增功能
• 仓库安家 /Users/maolong/Betty/草台班子/源码/（git 历史连续并入，无分叉）；确立"唯一源"原则：源码目录=唯一改动点，安装目录只被部署写入。
• scripts/部署.js：源→安装目录 rsync 镜像 + 发布检查一条命令；部署前拦截未提交改动。
• SKILL.md 新增"开发工作区"章节（1.8.2 起生效）。

### Bug 修复
• 修复部署脚本 ROOT 层级计算（脚本位于 skills/ctbz/scripts 需上四级）。
• 清理迁移期散落的根级 SKILL.md/scripts 残留。

## [1.8.2-p1] - 2026-09-10

反审硬门条款（用户指令）。

### 新增功能
• SKILL.md"反审硬门"条款：所有计划实施前必须走反审协议（规模匹配分级）；快速模式验证性产出与用户明示跳过为例外；倒计时/自动模式授权不得替代反审执行。

## [1.9.0] - 2026-09-10

吞噬 Betty：AGENTS.md 路由总入口化 + 知识库单库制 + 175 条建模知识迁入（3 路反审全裁）。

### 新增功能
• AGENTS.md 重写为路由总入口（所有任务调用 ctbz；风控建模四段式；前端 ui-pro-max；全局约定+优先级）。旧版双备份（.betty-on 时间戳），.codex 侧软链单源。
• SKILL.md 吸收 17 条：基础约束、领域四段式分流、计划五要素、反审六视角、验证证据清单、规则优先级（安全红线置顶）、文字经济学指令词表、归档互斥声明、.betty-record 只读+并行写出口。
• 知识库单库制：.ctbz/知识库/（用户级跨版本），建模知识库分区迁入 staging 185 条（Betty 主语全形态清洗，终验 0 残留），registry owner→ctbz。

### 优化改进
• 路由层新增领域分流（先于模式分流）。
• 反审硬门/优先级与既有红线体系咬合（吸收时去除 Agent 数冲突与归档双头）。

### Bug 修复
• 修复知识库版本目录断层（1.7.0 有知识库、1.8.2 无）——改为用户级单库。
• 修复 Betty 语境病句（29 条主语+来源+路径分批清洗，终验 0）。

## [1.9.1] - 2026-09-14

更正 + 读写落地 + 极简输出（含另一 Agent 实测报告的修复）。

### 更正声明
v1.9.0 提交实际只含 CHANGELOG（吸收条款误写安装副本被部署 rsync 抹除）。本版从会话记录重建全部条款进仓库树；部署.js 加反向检测防再犯。

### 新增功能
• 极简输出（全产物，含 description 硬约束句与指令词表；合并原"文字经济学"为单一条款）。
• 重建：反审硬门、规则优先级（红线置顶）、领域四段式分流、计划五要素、反审六视角、验证三禁。
• 收尾自记：任务收尾固定写自记库（做了什么/怎么做/坑），settings 可关。
• 背景摄取：新需求起草计划前必读 .ctbz-record（contract.md）。
• 知识库单库制（.ctbz/知识库/ 跨版本）+ 递归扫描兼容迁移条目格式（185 条可检索）+ 库指标写入 + 有效期解析与疑似过期标注。
• 部署.js 反向检测：安装侧有仓库没有的改动时拒绝部署。

### Bug 修复
• 版本号三处对齐（frontmatter/settings/发版.sh）。

## [1.9.2] - 2026-09-15

计划派发强制链（U1-U5 提案，来自另一机器复盘）+ activate 死锁修复。

### 新增功能
• 计划反审门含【新鲜 Agent 测试】硬问题；反审通过的计划只能派发，主会话不得自己执行（例外 <5 行即时修复）。
• 计划按任务契约组织（write-set/输入/验收），非叙事。
• 主会话 Edit/Write 前自问"该不该派发"；侦察后反模式警告。

### Bug 修复
• activate 死锁：installRoot 一致+指纹不一致=版本升级放行并刷新指纹；installRoot 不一致=副本劫持仍拦。

## [2.0.0] - 2026-09-15

治理与质量闭环版本。统一普通、快速、自动三种模式的工作流语义；增加计划、模块、集成与蓝图验收的反审协议，支持独立回执、反例验证和失败回修。发布清单明确唯一源码、历史归档边界及可追溯发布证据。

### 2.0.1 补充
- 增加 `自动更新检查.mjs`：供 Codex/ZCode 每日调度器检查 GitHub、快进更新并部署；检测到本地改动时安全停止。
